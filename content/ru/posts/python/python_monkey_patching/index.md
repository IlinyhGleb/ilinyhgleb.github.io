+++
date = '2025-11-17T14:00:02+05:00'
draft = true
title = 'Подмена функций в Monkey Patching в Python'
math = true
tags = ["Python", "informatika", "osnovi-programmirovaniya"]
categories = ['osnovi-programmirovaniya']
courses = ['osnovi-programmirovaniya']
weight = 7
+++

<!--more-->

## Monkey patching в Python: подмена импортированных функций на лету

Иногда нам нужно изменить поведение функции, которая нам не принадлежит: она импортируется из стороннего модуля, но менять её код невозможно, а переписывать все вызовы — неудобно. В таких ситуациях помогает классический приём **monkey patching** — подмена функции сразу после импорта.

Принцип прост:

1. импортируем оригинальную функцию под другим именем,
2. создаём свою обёртку,
3. переписываем имя исходной функции на новую версию.


Простой пример: допустим, у нас есть функция `greet` из модуля `some_module`:

```python
# В модуле some_module.py
def greet(name):
    print(f"Hello, {name}!")
```

Мы хотим, чтобы каждый вызов `greet` автоматически добавлял улыбку 🙂, но при этом **не менять исходный модуль**.

```python
from some_module import greet as original_greet

def greet(name):
    # добавляем смайлик к имени
    original_greet(f"{name} 🙂")

# Теперь весь код, который вызывает greet, использует новую версию
greet("Alice")  # вывод: Hello, Alice 🙂!
```

**Что мы сделали:**

1. Импортировали оригинальную функцию под другим именем (`original_greet`).
2. Создали новую функцию `greet`, которая добавляет поведение (см. смайлик).
3. Переписали имя функции, чтобы весь код продолжал вызывать `greet`, но уже с новым поведением.

---

Такой приём особенно полезен, когда:

* функция принадлежит сторонней библиотеке, и её код менять нельзя,
* нужно добавить предобработку или логирование,
* вы хотите временно изменить поведение функции для тестов или демонстраций.


